services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      secrets:
        - secrets
    image: phanvanluom97bd/crochet:latest
    ports:
      - "8080:8080"
    env_file:
      - .env.dev

secrets:
  secrets:
    file: ./secrets.txt
